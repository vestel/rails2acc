== Slides for Rails Bootcamp.

Build instructions

    $ git clone https://github.com/vestel/rails2acc
    $ cd rails2acc 
    $ bundle install
    $ slideshow install slidy
    $ slideshow build -t slidy <filename>.markdown
    $ open <filename>.html
